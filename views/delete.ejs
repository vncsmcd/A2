<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Set: <%= set.name %></title>
    <link href="/css/main.css" rel="stylesheet">
</head>

<body class="bg-gray-50 text-gray-800">
<%- include('partials/navbar', { page: '' }) %>

<div class="container mx-auto py-8 px-4">

    <!-- Hero Section -->
    <div class="hero bg-gradient-to-r from-red-400 to-red-600 text-white rounded-lg p-8 mb-8 text-center shadow-lg">
        <div class="max-w-lg mx-auto">
            <h1 class="text-5xl font-extrabold mb-4">Delete Set: <%= set.name %></h1>
            <p class="text-lg">
                Are you sure you want to delete the following Lego set from your collection? This action cannot be undone.
            </p>
        </div>
    </div>

    <!-- Set Details -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- Image -->
        <div>
            <img class="w-full rounded-lg shadow-md"
                src="<%= set.img_url %>"
                alt="Set Image" />
        </div>

        <!-- Details -->
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h2 class="text-2xl font-bold mb-4">Set Details</h2>
            <p class="text-lg mb-6">
                <span class="font-bold">Set Name: </span> <%= set.name %> <br />
                <span class="font-bold">Year Released: </span> <%= set.year %> <br />
                <span class="font-bold">Theme: </span> <%= set.Theme.name %> <br />
                <span class="font-bold">Number of Parts: </span> <%= set.num_parts %> <br />
            </p>

            <!-- Action Buttons -->
            <div class="flex gap-4">
                <!-- Confirm Delete Button -->
                <form action="/lego/deleteSet/<%= set.set_num %>" method="POST">
                    <button type="submit" class="btn btn-error px-6 py-3 text-lg font-medium shadow-md hover:shadow-lg transition">
                        CONFIRM DELETE
                    </button>
                </form>

                <!-- Cancel Button -->
                <a href="/lego/sets" class="btn btn-secondary px-6 py-3 text-lg font-medium shadow-md hover:shadow-lg transition">
                    CANCEL
                </a>
            </div>
        </div>
    </div>
</div>
</body>
</html>
