<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link href="/css/main.css" rel="stylesheet">
</head>

<body class="bg-gray-50 text-gray-800">
<%- include('partials/navbar', { page: '' }) %>

<div class="container mx-auto py-8 px-4">

    <!-- Hero Section -->
    <div class="hero bg-gradient-to-r from-green-400 via-blue-500 to-purple-500 text-white rounded-lg p-8 mb-8 text-center">
        <div class="max-w-lg mx-auto">
            <h1 class="text-4xl font-extrabold mb-4"><%= set.name %></h1>
            <p class="text-lg">
                Below, you will find detailed information about the set: <%= set.name %>.
            </p>
        </div>
    </div>

    <!-- Details Section -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- Image -->
        <div>
            <img class="w-full rounded-lg shadow-md"
                src="<%= set.img_url %>"
                alt="<%= set.name %>" />
        </div>

        <!-- Details -->
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h2 class="text-2xl font-bold mb-4">Details</h2>
            <p class="mb-4">
                <span class="font-bold">Set Name: </span> <%= set.name %> <br />
                <span class="font-bold">Year Released: </span> <%= set.year %> <br />
                <span class="font-bold">Theme: </span> <%= set.Theme.name %> <br />
                <span class="font-bold">Number of Parts: </span> <%= set.num_parts %> <br />
            </p>

            <!-- Action Buttons -->
            <div class="flex gap-4 mt-4">
                <!-- Return Button -->
                <a class="btn btn-primary" href="#" onclick="history.back(); return false;">RETURN</a>

                <!-- Edit Button -->
                <a class="btn btn-success" href="/lego/editSet/<%= set.set_num %>">EDIT</a>
            </div>
        </div>
    </div>
</div>
</body>
</html>
